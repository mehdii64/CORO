import type { FullReport } from "../types"
interface Props { full: FullReport }
export default function PreviewPanel(_: Props) { return <aside className="w-64 border-l bg-gray-50 p-4 text-xs text-gray-400">Aperçu — à implémenter</aside> }
